name: Manually triggered workflow

on:
  push:
    branches:
      - main



jobs:
  dev-deploy:
    name: Deploy-dev-deploy
    runs-on: ubuntu-latest
    steps:
    - name: Generate build ID
      if: ${{ github.event.label.name == 'skip-BG' }}
      id: prepasdfdf
      run: |
        echo ${{ github.event.label.name }}
    
    
